Act as Senior Frontend Engineer. Build this My Nest Chapter interactive HTML tool exactly to the approved design plan above.

Read CLAUDE.md and DESIGN.md in the repo root before touching any code.

Hard rules — no exceptions:
- Zero border-radius anywhere in the CSS
- Zero box-shadow anywhere in the CSS (no hardcoded shadows; no shadow tokens set to anything but none)
- Only Lora (display) + DM Sans (body) — no Montserrat, no Arial, no other fonts
- Only the locked colors from CLAUDE.md — no retired colors, no invented colors
- No emojis anywhere in the UI
- localStorage for all state — nothing resets on refresh
- Mobile-first — test every layout at 375px (iPhone SE) before calling it done

Build it now. When done, output a compliance checklist:
- List every CSS color value used and confirm each is in the locked palette
- Confirm zero instances of border-radius
- Confirm zero instances of box-shadow
- Confirm font stack is Lora + DM Sans only
